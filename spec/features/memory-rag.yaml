id: MEMORY-001
name: Memory and retrieval
priority: P0
requirements:
  - Ingested knowledge is persisted and retrieved through the domain boundary.
  - Retrieval failure is observable to callers.
acceptance_tests:
  - tests/test_phase6_memory_rag.py
  - tests/test_rag.py

