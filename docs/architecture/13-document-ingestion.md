# NovelForge Architecture V2：文档导入

支持 DOCX、Markdown、TXT。上传阶段验证扩展名、MIME、大小、文件名与项目归属，并保存原始附件。解析阶段提取规范文本、元数据、页/段落边界；分块阶段使用可追溯的字符范围与重叠策略；索引阶段建立 BM25/Embedding 记录。

用户必须选择或确认文档类型：世界观、写作技法、风格、背景、角色、既有正文、设定、参考小说。既有正文的导入走专门的 Chapter Import 审阅流，不能自动成为已提交事实。解析/索引失败为可重试 Task，并保留原附件和诊断信息。

