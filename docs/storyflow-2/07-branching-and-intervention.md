# Branching and Intervention

Branches fork at an immutable parent event sequence. Parent and sibling rows
are never updated by child execution. Author interventions are explicit
SimulationEvents with a rationale and state delta; they are sandbox evidence,
not Canon edits.
